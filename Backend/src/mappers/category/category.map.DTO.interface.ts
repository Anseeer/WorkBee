export interface ICategoryDTO {
    name: string,
    description: string,
    imageUrl?: string;
    isActive: boolean
}

export interface ICategoryEntity {
    name: string,
    description: string,
    imageUrl?: string;
    isActive: boolean
}